[versions]
dungeon-hub-api-client = "0.7.2"
hypixel-wrapper = "0.8.0"

[libraries]
dungeon-hub-api-client = { module = "net.dungeon-hub.api:client", version.ref = "dungeon-hub-api-client" }
hypixel-wrapper = { module = "net.dungeon-hub:hypixel-wrapper", version.ref = "hypixel-wrapper" }